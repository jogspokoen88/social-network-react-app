let initialState = {
  friends: [
    {name: "Anton", id: "1"},
    {name: "Stepan", id: "2"},
    {name: "Boris", id: "3"}
  ]
}

const friendsReducer = (state = initialState, action) => {

return state
}

export default friendsReducer;